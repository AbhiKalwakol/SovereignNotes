# SovereignNotes
A 100% offline, privacy-first AI personal assistant for summarizing notes, journals, and any personal files. Built for FOSS Hack 2026.

## Running the Mood Tracker UI
1. Install dependencies:

```bash
pip install -r requirements.txt
```

2. Run the Streamlit app:

```bash
streamlit run main.py
```
